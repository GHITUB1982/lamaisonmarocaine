<form method="post" action="{{ path('app_reglementation_crud_delete', {'id': reglementation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reglementation.id) }}">
    <button class="btn">Delete</button>
</form>
