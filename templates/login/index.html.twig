{# templates/login/index.html.twig #}
{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
    <div class="container form-group mb-5 mt-5">
        <h1 class="m-5 w-50">Login:</h1>
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form action="{{ path('app_login') }}" method="post">
        <label for="username">Email:</label>
        <input class="form-control m-2 w-50" type="text" id="username" name="_username" value="{{ last_username }}" required>

        <label for="password">Mot de passe:</label>
        <input class="form-control m-2 w-50 mb-5" type="password" id="password" name="_password" required>

        {# If you want to control the URL the user is redirected to on success #}
        <input type="hidden" name="_target_path" value="{{ path('app_account')}}">

        <button class="btn btn-success mb-5" type="submit">login</button>
    </form>
    <div>
        Vous avez oublié votre mot de passe? <a href="{{ path('app_forgot_password') }}">Réinitialiser le mot de passe</a>
                
        <br/>

        Vous n'avez pas de compte? <a href="{{ path('app_register') }}">Inscrivez-vous</a>

    </div>
{% endblock %}