{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1 class="mb-4 mt-5">Réinitialisation du mot de passe</h1>
    
    {{ form_start(form) }}
        {{ form_row(form.email) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}
</div>
{% endblock %}